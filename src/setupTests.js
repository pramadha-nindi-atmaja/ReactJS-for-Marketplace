/**
 * Extends Jest with custom DOM matchers from Testing Library.
 * Provides assertions like:
 *   expect(element).toHaveTextContent(/react/i)
 *
 * Docs: https://github.com/testing-library/jest-dom
 */
import "@testing-library/jest-dom";
